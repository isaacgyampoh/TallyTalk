/**
 * Backgrounds are intentionally clean (solid), not gradient. This component is
 * kept as a no-op so screens can opt into a backdrop later without refactoring.
 */
export function AuroraBg() {
  return null
}
